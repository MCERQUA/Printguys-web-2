# TASK T002: Run Prisma Migrations

## OBJECTIVE
Generate and run Prisma migrations to create database tables.

## PROJECT LOCATION
/home/nick/Nick/Printguys-AI/printguys-nextjs

## CONTEXT
- Database should be connected (T001 completed)
- Schema exists at /prisma/schema.prisma
- Need to create all tables defined in schema

## INSTRUCTIONS

1. Navigate to project directory:
```bash
cd /home/nick/Nick/Printguys-AI/printguys-nextjs
```

2. Generate Prisma client:
```bash
npx prisma generate
```

3. Create and run migration:
```bash
npx prisma migrate dev --name initial_schema
```

4. If migration fails, try db push instead:
```bash
npx prisma db push
```

5. Verify tables were created:
```bash
npx prisma studio &
# Or check via psql
PGPASSWORD=printguys_secure_2026 psql -U printguys -d printguys -h localhost -c "\dt"
```

6. Seed any initial data if needed (check for seed file in prisma/)

## OUTPUT
- Write results to: /home/nick/Nick/Printguys-AI/printguys-nextjs/z-code-outputs/agent1-T002.md
- List all tables created
- End with "TASK_COMPLETED" if successful or "TASK_FAILED: [reason]" if not

## CONSTRAINTS
- Never delete existing migrations
- Don't modify the schema
- Backup before running destructive operations
