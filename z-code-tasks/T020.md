# TASK T020: Add Google Analytics to All Pages

## OBJECTIVE
Set up Google Analytics 4 across the entire Next.js application.

## PROJECT LOCATION
/home/nick/Nick/Printguys-AI/printguys-nextjs

## INSTRUCTIONS

1. Add GA Measurement ID to .env.local:
```
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
```

2. Create GA component at /src/components/analytics/google-analytics.tsx:
```typescript
'use client'

import Script from 'next/script'

const GA_MEASUREMENT_ID = process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID

export function GoogleAnalytics() {
  if (!GA_MEASUREMENT_ID) return null

  return (
    <>
      <Script
        src={`https://www.googletagmanager.com/gtag/js?id=${GA_MEASUREMENT_ID}`}
        strategy="afterInteractive"
      />
      <Script id="google-analytics" strategy="afterInteractive">
        {`
          window.dataLayer = window.dataLayer || [];
          function gtag(){dataLayer.push(arguments);}
          gtag('js', new Date());
          gtag('config', '${GA_MEASUREMENT_ID}', {
            page_path: window.location.pathname,
          });
        `}
      </Script>
    </>
  )
}

// Event tracking helper
export function trackEvent(action: string, category: string, label?: string, value?: number) {
  if (typeof window !== 'undefined' && (window as any).gtag) {
    (window as any).gtag('event', action, {
      event_category: category,
      event_label: label,
      value: value,
    })
  }
}

// E-commerce tracking
export function trackPurchase(transactionId: string, value: number, items: any[]) {
  if (typeof window !== 'undefined' && (window as any).gtag) {
    (window as any).gtag('event', 'purchase', {
      transaction_id: transactionId,
      value: value,
      currency: 'CAD',
      items: items.map((item, index) => ({
        item_id: item.id,
        item_name: item.name,
        item_category: item.service,
        price: item.unitPrice,
        quantity: item.quantity,
        index: index,
      })),
    })
  }
}

// Add to cart tracking
export function trackAddToCart(item: any) {
  if (typeof window !== 'undefined' && (window as any).gtag) {
    (window as any).gtag('event', 'add_to_cart', {
      currency: 'CAD',
      value: item.totalPrice,
      items: [{
        item_id: item.id,
        item_name: item.name,
        item_category: item.service,
        price: item.unitPrice,
        quantity: item.quantity,
      }],
    })
  }
}
```

3. Add to root layout at /src/app/layout.tsx:
```typescript
import { GoogleAnalytics } from '@/components/analytics/google-analytics'

// In the body:
<GoogleAnalytics />
```

4. Track key events:
- Add tracking to cart additions
- Add tracking to checkout
- Add tracking to contact form submissions
- Add tracking to design saves

5. Example usage in cart store:
```typescript
import { trackAddToCart } from '@/components/analytics/google-analytics'

// In addItem action:
addItem: (item) => {
  const newItem = { ...item, id: `cart-${Date.now()}` }
  trackAddToCart(newItem)
  set((state) => ({ items: [...state.items, newItem] }))
}
```

## OUTPUT
- Write results to: /home/nick/Nick/Printguys-AI/printguys-nextjs/z-code-outputs/agent8-T020.md
- End with "TASK_COMPLETED" if successful
