# TASK T022: Optimize Images

## OBJECTIVE
Optimize all images for better performance.

## PROJECT LOCATION
/home/nick/Nick/Printguys-AI/printguys-nextjs

## INSTRUCTIONS

1. Find all images in the project:
```bash
find /home/nick/Nick/Printguys-AI/printguys-nextjs/public -type f \( -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.webp" \) -exec ls -lh {} \;
```

2. Identify large images (>200KB):
```bash
find /home/nick/Nick/Printguys-AI/printguys-nextjs/public -type f \( -name "*.png" -o -name "*.jpg" \) -size +200k
```

3. Optimize images using available tools:
```bash
# For PNG files
for f in $(find /home/nick/Nick/Printguys-AI/printguys-nextjs/public -name "*.png" -size +200k); do
  convert "$f" -strip -quality 85 "${f%.png}.optimized.png"
  mv "${f%.png}.optimized.png" "$f"
done

# For JPG files
for f in $(find /home/nick/Nick/Printguys-AI/printguys-nextjs/public -name "*.jpg" -size +200k); do
  convert "$f" -strip -quality 85 "$f"
done
```

4. Convert large PNG to WebP where appropriate:
```bash
# Create WebP versions
for f in $(find /home/nick/Nick/Printguys-AI/printguys-nextjs/public -name "*.png" -size +100k); do
  cwebp -q 80 "$f" -o "${f%.png}.webp"
done
```

5. Update Next.js config for image optimization in next.config.ts:
```typescript
const nextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],
    imageSizes: [16, 32, 48, 64, 96, 128, 256],
    minimumCacheTTL: 60 * 60 * 24 * 30, // 30 days
  },
}
```

6. Ensure all <img> tags use Next.js Image component:
- Search for raw <img> tags
- Convert to <Image> from next/image
- Add width, height, and alt props

7. Add blur placeholders for large images:
```typescript
import Image from 'next/image'

<Image
  src="/hero-image.jpg"
  alt="Hero"
  width={1920}
  height={1080}
  placeholder="blur"
  blurDataURL="data:image/jpeg;base64,..."
  priority // for above-fold images
/>
```

## OUTPUT
- Write results to: /home/nick/Nick/Printguys-AI/printguys-nextjs/z-code-outputs/agent8-T022.md
- List all images optimized
- Note size reductions achieved
- End with "TASK_COMPLETED" if successful
